diff --git a/apps/desktop/src/app/chat/sidebar/filter-menu.tsx b/apps/desktop/src/app/chat/sidebar/filter-menu.tsx
index f7019c3a8f..08e790afbb 100644
--- a/apps/desktop/src/app/chat/sidebar/filter-menu.tsx
+++ b/apps/desktop/src/app/chat/sidebar/filter-menu.tsx
@@ -365,7 +365,7 @@ export function SidebarFilterMenu({ className }: { className?: string }) {
           <OptionCheckbox
             checked={showArchived}
             onCheck={() => setSidebarShowArchived(!showArchived)}
-            option={{ id: 'archived', label: 'Archived' }}
+            option={{ id: 'archived', label: '╨Т ╨░╤А╤Е╨╕╨▓╨╡' }}
           />
 
           {/* One way back rather than two near-identical ones: this drops the
diff --git a/apps/desktop/src/app/contrib/controller.tsx b/apps/desktop/src/app/contrib/controller.tsx
index 0210188e23..7761cd62b6 100644
--- a/apps/desktop/src/app/contrib/controller.tsx
+++ b/apps/desktop/src/app/contrib/controller.tsx
@@ -386,7 +386,7 @@ const QUAD_TREE = split(
 registry.registerMany([
   { id: 'default', area: 'layouts', title: 'Default', order: 0, data: DEFAULT_TREE },
   { id: 'focus', area: 'layouts', title: 'Focus', order: 10, data: FOCUS_TREE },
-  { id: 'terminal-deck', area: 'layouts', title: 'Terminal deck', order: 20, data: TERMINAL_TREE },
+  { id: 'terminal-deck', area: 'layouts', title: '╨в╨╡╤А╨╝╨╕╨╜╨░╨╗', order: 20, data: TERMINAL_TREE },
   { id: 'quad', area: 'layouts', title: 'Quad', order: 30, data: QUAD_TREE }
 ])
 
diff --git a/apps/desktop/src/app/quick-entry/quick-entry-app.tsx b/apps/desktop/src/app/quick-entry/quick-entry-app.tsx
index 3f5d54835d..57b9c198f7 100644
--- a/apps/desktop/src/app/quick-entry/quick-entry-app.tsx
+++ b/apps/desktop/src/app/quick-entry/quick-entry-app.tsx
@@ -182,8 +182,8 @@ export function QuickEntryApp() {
             }}
             value={state.target}
           >
-            <option value={QUICK_TARGET_CURRENT}>Current chat</option>
-            <option value={QUICK_TARGET_NEW}>New session</option>
+            <option value={QUICK_TARGET_CURRENT}>╨в╨╡╨║╤Г╤Й╨╕╨╣ ╤З╨░╤В</option>
+            <option value={QUICK_TARGET_NEW}>╨Э╨╛╨▓╨░╤П ╤Б╨╡╤Б╤Б╨╕╤П</option>
             {state.sessions.map(session => (
               <option key={session.id} value={session.id}>
                 {session.title}
diff --git a/apps/desktop/src/app/settings/custom-endpoints-settings.tsx b/apps/desktop/src/app/settings/custom-endpoints-settings.tsx
index b74b30a290..3d1667ac12 100644
--- a/apps/desktop/src/app/settings/custom-endpoints-settings.tsx
+++ b/apps/desktop/src/app/settings/custom-endpoints-settings.tsx
@@ -11,6 +11,7 @@ import {
   validateCustomEndpoint
 } from '@/hermes'
 import { triggerHaptic } from '@/lib/haptics'
+import { useI18n } from '@/i18n'
 import { Check, Globe, Loader2, Plus, Save, Trash2, Zap } from '@/lib/icons'
 import { cn } from '@/lib/utils'
 import { notify, notifyError } from '@/store/notifications'
@@ -141,9 +142,9 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
 
       triggerHaptic('success')
       onConfigSaved?.()
-      notify({ kind: 'success', message: 'Custom endpoint saved.' })
+      notify({ kind: 'success', message: '╨н╨╜╨┤╨┐╨╛╨╕╨╜╤В ╤Б╨╛╤Е╤А╨░╨╜╤С╨╜.' })
     } catch (err) {
-      notifyError(err, 'Save failed')
+      notifyError(err, '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╤Б╨╛╤Е╤А╨░╨╜╨╕╤В╤М')
     } finally {
       setSaving(false)
     }
@@ -163,8 +164,8 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
         notify({
           kind: 'success',
           message: response.models.length
-            ? `Endpoint is reachable. Found ${response.models.length} models.`
-            : 'Endpoint is reachable.'
+            ? `╨н╨╜╨┤╨┐╨╛╨╕╨╜╤В ╨┤╨╛╤Б╤В╤Г╨┐╨╡╨╜. ╨Э╨░╨╣╨┤╨╡╨╜╨╛ ╨╝╨╛╨┤╨╡╨╗╨╡╨╣: ${response.models.length}.`
+            : '╨н╨╜╨┤╨┐╨╛╨╕╨╜╤В ╨┤╨╛╤Б╤В╤Г╨┐╨╡╨╜.'
         })
       } else {
         notify({
@@ -173,7 +174,7 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
         })
       }
     } catch (err) {
-      notifyError(err, 'Validation failed')
+      notifyError(err, '╨Ю╤И╨╕╨▒╨║╨░ ╨┐╤А╨╛╨▓╨╡╤А╨║╨╕')
     } finally {
       setTesting(false)
     }
@@ -188,14 +189,14 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
       onMainModelChanged?.(response.provider, response.model)
       triggerHaptic('success')
     } catch (err) {
-      notifyError(err, 'Activation failed')
+      notifyError(err, '╨Ю╤И╨╕╨▒╨║╨░ ╨░╨║╤В╨╕╨▓╨░╤Ж╨╕╨╕')
     } finally {
       setActivating(null)
     }
   }
 
   async function handleDelete(endpoint: CustomEndpoint) {
-    if (!window.confirm(`Delete ${endpoint.name}?`)) {
+    if (!window.confirm(`╨г╨┤╨░╨╗╨╕╤В╤М ${endpoint.name}?`)) {
       return
     }
 
@@ -212,7 +213,7 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
       onConfigSaved?.()
       triggerHaptic('success')
     } catch (err) {
-      notifyError(err, 'Delete failed')
+      notifyError(err, '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╤Г╨┤╨░╨╗╨╕╤В╤М')
     } finally {
       setDeleting(null)
     }
@@ -229,7 +230,7 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
     <SettingsContent>
       <div className="space-y-6">
         <section>
-          <SectionHeading icon={Globe} meta={`${endpoints.length}`} title="Custom Endpoints" />
+          <SectionHeading icon={Globe} meta={`${endpoints.length}`} title="╨Я╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М╤Б╨║╨╕╨╡ ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В╤Л" />
           <div className="divide-y divide-border/40 rounded-md border border-border/50">
             {endpoints.length ? (
               endpoints.map(endpoint => (
@@ -257,7 +258,7 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
                     </div>
                     <div className="mt-1 flex flex-wrap gap-2 text-xs text-muted-foreground">
                       <span>{endpoint.model}</span>
-                      {endpoint.has_api_key && <span>{endpoint.api_key_preview ?? 'API key set'}</span>}
+                      {endpoint.has_api_key && <span>{endpoint.api_key_preview ?? 'API-╨║╨╗╤О╤З ╨╖╨░╨┤╨░╨╜'}</span>}
                     </div>
                   </button>
                   <div className="flex items-center gap-2 sm:justify-end">
@@ -276,7 +277,7 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
                         disabled={deleting === endpoint.id}
                         onClick={() => void handleDelete(endpoint)}
                         size="icon-sm"
-                        title="Delete endpoint"
+                        title="╨г╨┤╨░╨╗╨╕╤В╤М ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В"
                         variant="ghost"
                       >
                         {deleting === endpoint.id ? <Loader2 className="animate-spin" /> : <Trash2 />}
@@ -286,13 +287,13 @@ export function CustomEndpointsSettings({ onConfigSaved, onMainModelChanged }: C
                 </div>
               ))
             ) : (
-              <EmptyState description="Add an OpenAI-compatible endpoint below." title="No custom endpoints" />
+              <EmptyState description="╨Ф╨╛╨▒╨░╨▓╤М╤В╨╡ OpenAI-╤Б╨╛╨▓╨╝╨╡╤Б╤В╨╕╨╝╤Л╨╣ ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В ╨╜╨╕╨╢╨╡." title="╨Э╨╡╤В ╨┐╨╛╨╗╤М╨╖╨╛╨▓╨░╤В╨╡╨╗╤М╤Б╨║╨╕╤Е ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В╨╛╨▓" />
             )}
           </div>
         </section>
 
         <section>
-          <SectionHeading icon={Plus} title={form.id ? 'Edit Endpoint' : 'Add Endpoint'} />
+          <SectionHeading icon={Plus} title={form.id ? '╨Ш╨╖╨╝╨╡╨╜╨╕╤В╤М ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В' : '╨Ф╨╛╨▒╨░╨▓╨╕╤В╤М ╤Н╨╜╨┤╨┐╨╛╨╕╨╜╤В'} />
           <div className="grid gap-3 rounded-md border border-border/50 p-3">
             <div className="grid gap-3 sm:grid-cols-2">
               <label className="grid gap-1.5 text-xs text-muted-foreground">
diff --git a/apps/desktop/src/app/settings/model-settings.tsx b/apps/desktop/src/app/settings/model-settings.tsx
index fd19d165c4..132f093719 100644
--- a/apps/desktop/src/app/settings/model-settings.tsx
+++ b/apps/desktop/src/app/settings/model-settings.tsx
@@ -167,11 +167,13 @@ function StaleAuxWarning({ applying, onReset, slots, taskLabel }: StaleAuxWarnin
     <div className="flex flex-wrap items-center gap-2 rounded-md border border-amber-500/40 bg-amber-500/10 px-3 py-2 text-xs text-amber-200">
       <AlertTriangle className="size-3.5 shrink-0" />
       <span className="grow">
-        {slots.length} auxiliary task{slots.length === 1 ? '' : 's'} ({names}) still run on{' '}
-        <span className="font-mono">{allSameProvider ? provider : 'other providers'}</span>, not your main model.
+        {slots.length === 1
+          ? `╨Т╤Б╨┐╨╛╨╝╨╛╨│╨░╤В╨╡╨╗╤М╨╜╨░╤П ╨╖╨░╨┤╨░╤З╨░ (${names}) ╨▓╤Б╤С ╨╡╤Й╤С ╤А╨░╨▒╨╛╤В╨░╨╡╤В ╨╜╨░ `
+          : `╨Т╤Б╨┐╨╛╨╝╨╛╨│╨░╤В╨╡╨╗╤М╨╜╤Л╨╡ ╨╖╨░╨┤╨░╤З╨╕ (${names}) ╨▓╤Б╤С ╨╡╤Й╤С ╤А╨░╨▒╨╛╤В╨░╤О╤В ╨╜╨░ `}
+        <span className="font-mono">{allSameProvider ? provider : '╨┤╤А╤Г╨│╨╕╤Е ╨┐╤А╨╛╨▓╨░╨╣╨┤╨╡╤А╨░╤Е'}</span>, ╨╜╨╡ ╨╜╨░ ╨╛╤Б╨╜╨╛╨▓╨╜╨╛╨╣ ╨╝╨╛╨┤╨╡╨╗╨╕.
       </span>
       <Button disabled={applying} onClick={onReset} size="sm" variant="textStrong">
-        Reset all to main
+        ╨б╨▒╤А╨╛╤Б╨╕╤В╤М ╨╜╨░ ╨╛╤Б╨╜╨╛╨▓╨╜╤Г╤О
       </Button>
     </div>
   )
diff --git a/apps/desktop/src/app/settings/plugins-settings.tsx b/apps/desktop/src/app/settings/plugins-settings.tsx
index ca31ff3838..b31dbfebe1 100644
--- a/apps/desktop/src/app/settings/plugins-settings.tsx
+++ b/apps/desktop/src/app/settings/plugins-settings.tsx
@@ -59,7 +59,7 @@ async function revealPluginsDir() {
     const dir = await window.hermesDesktop?.desktopPluginsRoot?.()
 
     if (!dir) {
-      notifyError('Desktop plugins are unavailable', 'Could not resolve the plugins folder')
+      notifyError('╨Я╨╗╨░╨│╨╕╨╜╤Л ╤А╨░╨▒╨╛╤З╨╡╨│╨╛ ╤Б╤В╨╛╨╗╨░ ╨╜╨╡╨┤╨╛╤Б╤В╤Г╨┐╨╜╤Л', '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╨┐╤А╨╡╨┤╨╡╨╗╨╕╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
 
       return
     }
@@ -69,10 +69,10 @@ async function revealPluginsDir() {
     const result = await window.hermesDesktop?.openDir?.(dir)
 
     if (result && !result.ok) {
-      notifyError(result.error ?? 'unknown error', 'Could not open the plugins folder')
+      notifyError(result.error ?? '╨╜╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░', '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╤В╨║╤А╤Л╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
     }
   } catch (err) {
-    notifyError(err, 'Could not resolve the plugins folder')
+    notifyError(err, '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╨┐╤А╨╡╨┤╨╡╨╗╨╕╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
   }
 }
 
@@ -86,7 +86,7 @@ async function revealAgentPluginsDir(request: GatewayRequest) {
     const home = (result?.home ?? '').trim()
 
     if (!home) {
-      notifyError('The backend did not report its home directory', 'Could not open the plugins folder')
+      notifyError('╨С╤Н╨║╨╡╨╜╨┤ ╨╜╨╡ ╤Б╨╛╨╛╨▒╤Й╨╕╨╗ ╨┤╨╛╨╝╨░╤И╨╜╨╕╨╣ ╨║╨░╤В╨░╨╗╨╛╨│', '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╤В╨║╤А╤Л╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
 
       return
     }
@@ -94,10 +94,10 @@ async function revealAgentPluginsDir(request: GatewayRequest) {
     const opened = await window.hermesDesktop?.openDir?.(`${home}/plugins`)
 
     if (opened && !opened.ok) {
-      notifyError(opened.error ?? 'unknown error', 'Could not open the plugins folder')
+      notifyError(opened.error ?? '╨╜╨╡╨╕╨╖╨▓╨╡╤Б╤В╨╜╨░╤П ╨╛╤И╨╕╨▒╨║╨░', '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╤В╨║╤А╤Л╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
     }
   } catch (err) {
-    notifyError(err, 'Could not open the plugins folder')
+    notifyError(err, '╨Э╨╡ ╤Г╨┤╨░╨╗╨╛╤Б╤М ╨╛╤В╨║╤А╤Л╤В╤М ╨┐╨░╨┐╨║╤Г ╨┐╨╗╨░╨│╨╕╨╜╨╛╨▓')
   }
 }
 
diff --git a/apps/desktop/src/components/assistant-ui/markdown-text.tsx b/apps/desktop/src/components/assistant-ui/markdown-text.tsx
index e6245d8584..b5b9784e31 100644
--- a/apps/desktop/src/components/assistant-ui/markdown-text.tsx
+++ b/apps/desktop/src/components/assistant-ui/markdown-text.tsx
@@ -230,7 +230,7 @@ function MediaAttachment({ path }: { path: string }) {
           open()
         }}
       >
-        {failed ? `Open ${name}` : `Loading ${name}...`}
+        {failed ? `Open ${name}` : `╨Ч╨░╨│╤А╤Г╨╖╨║╨░ ${name}...`}
       </a>
       {openFailed && <OpenMediaFailedNote name={name} />}
     </span>
diff --git a/apps/desktop/src/components/assistant-ui/thread/assistant-message.tsx b/apps/desktop/src/components/assistant-ui/thread/assistant-message.tsx
index 043a60401c..75b5392e68 100644
--- a/apps/desktop/src/components/assistant-ui/thread/assistant-message.tsx
+++ b/apps/desktop/src/components/assistant-ui/thread/assistant-message.tsx
@@ -18,7 +18,7 @@ import {
 import { MESSAGE_PARTS_COMPONENTS } from '@/components/assistant-ui/thread/message-parts'
 import { ReactionPicker } from '@/components/assistant-ui/thread/message-reactions'
 import { ResponseLoadingIndicator, StreamStallIndicator } from '@/components/assistant-ui/thread/status'
-import { formatMessageTimestamp } from '@/components/assistant-ui/thread/timestamp'
+import { MessageTimestamp } from '@/components/assistant-ui/thread/message-timestamp'
 import { useMessageReactions, useTapbackDoubleClick } from '@/components/assistant-ui/thread/use-message-reactions'
 import { TooltipIconButton } from '@/components/assistant-ui/tooltip-icon-button'
 import { PreviewAttachment } from '@/components/chat/preview-attachment'
@@ -28,7 +28,6 @@ import { useI18n } from '@/i18n'
 import { triggerHaptic } from '@/lib/haptics'
 import { AudioLines, GitForkIcon, Loader2Icon, RefreshCwIcon, SmilePlusIcon, VolumeXIcon, XIcon } from '@/lib/icons'
 import { extractPreviewTargets } from '@/lib/preview-targets'
-import { formatAgo } from '@/lib/time'
 import { useEnterAnimation } from '@/lib/use-enter-animation'
 import { cn } from '@/lib/utils'
 import { playSpeechText, stopVoicePlayback } from '@/lib/voice-playback'
@@ -198,7 +197,6 @@ const AssistantActionBar: FC<MessageActionProps> = ({ messageId, getMessageText,
         }
         data-slot="aui_msg-actions"
       >
-        <MessageAge />
         {onBranchInNewChat && (
           <TooltipIconButton
             onClick={() => {
@@ -300,28 +298,11 @@ const ReadAloudButton: FC<{ getText: () => string; messageId: string }> = ({ get
   )
 }
 
-const MessageAge: FC = () => {
-  const { t } = useI18n()
-  const createdAt = useAuiState(s => s.message.createdAt)
-  const date = createdAt ? new Date(createdAt) : null
-
-  if (!date || Number.isNaN(date.getTime())) {
-    return null
-  }
-
-  // Compact "2h ago" (shared util) with the absolute time on hover.
-  return (
-    <span
-      className="px-0.5 text-[0.6875rem] tabular-nums text-muted-foreground"
-      title={formatMessageTimestamp(date, t.assistant.thread) || undefined}
-    >
-      {formatAgo(date.getTime(), t.agents)}
-    </span>
-  )
-}
-
 const AssistantFooter: FC<MessageActionProps> = props => (
   <div className="flex min-h-6 flex-col items-end gap-1 pr-(--message-text-indent) pl-(--message-text-indent)">
+    {/* Mod: hermes-desktop-timestamps тАФ always-visible timestamp; stock
+        shows the age only inside the hover-revealed action bar. */}
+    <MessageTimestamp />
     <BranchPickerPrimitive.Root
       className="inline-flex h-6 items-center gap-1 text-xs text-muted-foreground"
       hideWhenSingleBranch
diff --git a/apps/desktop/src/components/assistant-ui/thread/user-message.tsx b/apps/desktop/src/components/assistant-ui/thread/user-message.tsx
index e71d189428..14079e8dd6 100644
--- a/apps/desktop/src/components/assistant-ui/thread/user-message.tsx
+++ b/apps/desktop/src/components/assistant-ui/thread/user-message.tsx
@@ -3,6 +3,7 @@ import { type FC, type ReactNode, useCallback, useRef, useState } from 'react'
 
 import { DirectiveContent } from '@/components/assistant-ui/directive-text'
 import { messageAttachmentRefs, messageContentText } from '@/components/assistant-ui/thread/content'
+import { MessageTimestamp } from '@/components/assistant-ui/thread/message-timestamp'
 import { ReactionBadge, ReactionPicker } from '@/components/assistant-ui/thread/message-reactions'
 import { type RestoreMessageTarget } from '@/components/assistant-ui/thread/types'
 import { useMessageReactions } from '@/components/assistant-ui/thread/use-message-reactions'
@@ -405,6 +406,9 @@ export const UserMessage: FC<{
               onRetract={() => react(null)}
               reactions={shownReactions}
             />
+            {/* Mod: hermes-desktop-timestamps тАФ stock shows no time at all
+                for user messages; the timestamp lives below the bubble. */}
+            <MessageTimestamp className="self-end py-1.5 pr-1.5" />
             <BranchPickerPrimitive.Root
               className={cn(
                 'checkpoint-container flex items-center gap-1 pb-0 pt-1 pl-1.5 text-[0.75rem] leading-none text-(--ui-text-tertiary)',
diff --git a/apps/desktop/src/components/assistant-ui/tool/fallback.tsx b/apps/desktop/src/components/assistant-ui/tool/fallback.tsx
index 6e56d0918c..3a92f9c9cf 100644
--- a/apps/desktop/src/components/assistant-ui/tool/fallback.tsx
+++ b/apps/desktop/src/components/assistant-ui/tool/fallback.tsx
@@ -615,7 +615,7 @@ function ToolEntry({ part }: ToolEntryProps) {
             <div className="max-w-full text-xs leading-relaxed text-(--ui-text-secondary)">
               {view.searchQuery && (
                 <p className="mb-1 flex min-w-0 gap-1.5 wrap-anywhere">
-                  <span className="shrink-0 font-medium text-(--ui-text-tertiary)">Search</span>
+                  <span className="shrink-0 font-medium text-(--ui-text-tertiary)">╨Я╨╛╨╕╤Б╨║</span>
                   <span>{view.searchQuery}</span>
                 </p>
               )}
diff --git a/apps/desktop/src/components/chat/intro.tsx b/apps/desktop/src/components/chat/intro.tsx
index 6dc365e644..6839eee8b3 100644
--- a/apps/desktop/src/components/chat/intro.tsx
+++ b/apps/desktop/src/components/chat/intro.tsx
@@ -22,24 +22,24 @@ const NEUTRAL_PERSONALITIES = new Set(['', 'default', 'none', 'neutral'])
 
 const FALLBACK_COPY: IntroCopy[] = [
   {
-    headline: 'What are we moving today?',
-    body: "Send a bug, branch, plan, or rough idea. I'll inspect the repo and turn it into the next concrete step."
+    headline: '╨з╤В╨╛ ╤Б╨╡╨│╨╛╨┤╨╜╤П ╨┤╨▓╨╕╨│╨░╨╡╨╝?',
+    body: '╨Я╤А╨╕╤И╨╗╨╕╤В╨╡ ╨▒╨░╨│, ╨▓╨╡╤В╨║╤Г, ╨┐╨╗╨░╨╜ ╨╕╨╗╨╕ ╤Б╤Л╤А╤Г╤О ╨╕╨┤╨╡╤О. ╨п ╨╕╨╖╤Г╤З╤Г ╤А╨╡╨┐╨╛╨╖╨╕╤В╨╛╤А╨╕╨╣ ╨╕ ╨┐╤А╨╡╨▓╤А╨░╤Й╤Г ╤Н╤В╨╛ ╨▓ ╨║╨╛╨╜╨║╤А╨╡╤В╨╜╤Л╨╣ ╤Б╨╗╨╡╨┤╤Г╤О╤Й╨╕╨╣ ╤И╨░╨│.'
   },
   {
-    headline: "What's on your mind?",
-    body: "Bring the code, question, or stuck part. I'll read the room before making changes."
+    headline: '╨з╤В╨╛ ╤Г ╤В╨╡╨▒╤П ╨╜╨░ ╤Г╨╝╨╡?',
+    body: '╨Я╤А╨╕╨╜╨╡╤Б╨╕╤В╨╡ ╨║╨╛╨┤, ╨▓╨╛╨┐╤А╨╛╤Б ╨╕╨╗╨╕ ╨╖╨░╤Б╤В╤А╤П╨▓╤И╨╡╨╡ ╨╝╨╡╤Б╤В╨╛. ╨п ╤Б╨╜╨░╤З╨░╨╗╨░ ╤А╨░╨╖╨▒╨╡╤А╤Г╤Б╤М, ╨┐╤А╨╡╨╢╨┤╨╡ ╤З╨╡╨╝ ╤З╤В╨╛-╤В╨╛ ╨╝╨╡╨╜╤П╤В╤М.'
   },
   {
-    headline: 'What should Hermes look at?',
-    body: "Send the task, failing path, or half-formed plan. I'll help turn it into action."
+    headline: '╨Э╨░ ╤З╤В╨╛ Hermes ╨┤╨╛╨╗╨╢╨╡╨╜ ╨┐╨╛╤Б╨╝╨╛╤В╤А╨╡╤В╤М?',
+    body: '╨Я╤А╨╕╤И╨╗╨╕╤В╨╡ ╨╖╨░╨┤╨░╤З╤Г, ╨┐╨░╨┤╨░╤О╤Й╨╕╨╣ ╨┐╤Г╤В╤М ╨╕╨╗╨╕ ╨┐╨╛╨╗╤Г╨╛╤Д╨╛╤А╨╝╨╗╨╡╨╜╨╜╤Л╨╣ ╨┐╨╗╨░╨╜. ╨Я╨╛╨╝╨╛╨│╤Г ╨┐╤А╨╡╨▓╤А╨░╤В╨╕╤В╤М ╤Н╤В╨╛ ╨▓ ╨┤╨╡╨╣╤Б╤В╨▓╨╕╨╡.'
   },
   {
-    headline: 'Where should we start?',
-    body: "Bring the problem, goal, or file. I'll inspect first and keep the next step concrete."
+    headline: '╨б ╤З╨╡╨│╨╛ ╨╜╨░╤З╨╜╤С╨╝?',
+    body: '╨Я╤А╨╕╨╜╨╡╤Б╨╕╤В╨╡ ╨┐╤А╨╛╨▒╨╗╨╡╨╝╤Г, ╤Ж╨╡╨╗╤М ╨╕╨╗╨╕ ╤Д╨░╨╣╨╗. ╨п ╤Б╨╜╨░╤З╨░╨╗╨░ ╨╛╤Б╨╝╨╛╤В╤А╤О╤Б╤М ╨╕ ╤Б╨┤╨╡╨╗╨░╤О ╤Б╨╗╨╡╨┤╤Г╤О╤Й╨╕╨╣ ╤И╨░╨│ ╨║╨╛╨╜╨║╤А╨╡╤В╨╜╤Л╨╝.'
   },
   {
-    headline: 'What needs attention?',
-    body: "Send the context you have. I'll help sort it into a plan or a fix."
+    headline: '╨з╤В╨╛ ╤В╤А╨╡╨▒╤Г╨╡╤В ╨▓╨╜╨╕╨╝╨░╨╜╨╕╤П?',
+    body: '╨Я╤А╨╕╤И╨╗╨╕╤В╨╡ ╨╕╨╝╨╡╤О╤Й╨╕╨╣╤Б╤П ╨║╨╛╨╜╤В╨╡╨║╤Б╤В. ╨Я╨╛╨╝╨╛╨│╤Г ╤А╨░╨╖╨╗╨╛╨╢╨╕╤В╤М ╨╡╨│╨╛ ╨▓ ╨┐╨╗╨░╨╜ ╨╕╨╗╨╕ ╨╕╤Б╨┐╤А╨░╨▓╨╗╨╡╨╜╨╕╨╡.'
   }
 ]
 
diff --git a/apps/desktop/src/i18n/catalog.ts b/apps/desktop/src/i18n/catalog.ts
index f35c957076..c6f5be8285 100644
--- a/apps/desktop/src/i18n/catalog.ts
+++ b/apps/desktop/src/i18n/catalog.ts
@@ -1,6 +1,8 @@
 import { ar } from './ar'
 import { en } from './en'
 import { ja } from './ja'
+// MOD: hermes-desktop-ru тАФ Russian locale
+import { ru } from './ru'
 import type { Locale, Translations } from './types'
 import { zh } from './zh'
 import { zhHant } from './zh-hant'
@@ -10,5 +12,7 @@ export const TRANSLATIONS: Record<Locale, Translations> = {
   zh,
   'zh-hant': zhHant,
   ja,
-  ar
+  ar,
+  // MOD: hermes-desktop-ru
+  ru
 }
diff --git a/apps/desktop/src/i18n/en.ts b/apps/desktop/src/i18n/en.ts
index da7386f185..c157ac5176 100644
--- a/apps/desktop/src/i18n/en.ts
+++ b/apps/desktop/src/i18n/en.ts
@@ -42,7 +42,11 @@ export const en: Translations = {
     update: 'Update',
     tryHint: term => `Try тАЬ${term}тАЭ`,
     on: 'On',
-    off: 'Off'
+    off: 'Off',
+    use: 'Use',
+    dismiss: 'Dismiss',
+    showOptions: 'Show options',
+    moreActions: 'More actions',
   },
 
   fileMenu: {
@@ -366,6 +370,7 @@ export const en: Translations = {
       disable: 'Disable',
       failed: 'failed',
       empty: 'No desktop plugins installed yet.',
+      kanban: 'Multi-agent task board тАФ board page, sidebar entry, and a live in-flight count in the status bar.',
       kinds: { bundled: 'bundled', disk: 'on disk', runtime: 'runtime' },
       agent: {
         title: 'Agent plugins',
@@ -379,6 +384,9 @@ export const en: Translations = {
         toggleFailed: (name: string) => `Could not toggle ${name}`,
         updateBackendToManage: 'Update the Hermes backend to manage this plugin from Desktop.',
         sources: { bundled: 'bundled', user: 'user', git: 'git', project: 'project', entrypoint: 'pip' }
+      },
+      descriptions: {
+      kanban: 'Multi-agent task board тАФ board page, sidebar entry, and a live in-flight count in the status bar.'
       }
     },
     notifications: {
@@ -533,7 +541,8 @@ export const en: Translations = {
         noneAvailable: 'No pets available to turn on right now.',
         turnOnFailed: 'Could not turn the pet on.',
         turnOffFailed: 'Could not turn the pet off.'
-      }
+      },
+      searchThemes: 'Search your themes or the VS Code MarketplaceтАж',
     },
     fieldLabels: FIELD_LABELS,
     fieldDescriptions: FIELD_DESCRIPTIONS,
@@ -597,7 +606,10 @@ export const en: Translations = {
       shortcutDesc: 'Needs at least one modifier, e.g. CommandOrControl+Shift+Space.',
       active: 'Shortcut is active.',
       takenBy: 'Another app already uses this shortcut тАФ pick a different one.',
-      invalidShortcut: 'Not a valid shortcut. Include at least one modifier key.'
+      invalidShortcut: 'Not a valid shortcut. Include at least one modifier key.',
+      windowAriaLabel: 'Quick Entry',
+      newTarget: 'New session',
+      currentTarget: 'Current chat',
     },
     credentials: {
       pasteKey: 'Paste key',
@@ -999,7 +1011,105 @@ export const en: Translations = {
         failedSelect: backend => `Failed to select ${backend}`,
         needsSetupHint: 'You can select this backend now тАФ commands will fail until setup is complete.'
       }
-    }
+    },
+    computerUse: {
+      accessibility: 'Accessibility',
+      accessibilityHint: 'Lets cua-driver post clicks, keystrokes, and read the accessibility tree.',
+      approveMessage:
+        'macOS will show a permission dialog attributed to CuaDriver. Approve it, then return here.',
+      approveTitle: 'Approve in System Settings',
+      checking: 'Checking Computer Use statusтАж',
+      driverHealth: 'Driver health',
+      errorRead: 'Could not read Computer Use status',
+      errorRequest: 'Could not request permissions',
+      grantPermissions: 'Grant permissions',
+      granted: 'Granted',
+      grantsNote:
+        "Grants attach to CuaDriver's own identity (com.trycua.driver), not Hermes тАФ so the dialog is attributed to the process that drives your Mac.",
+      notGranted: 'Not granted',
+      notInstalled: 'Install the cua-driver backend below to drive this machine.',
+      notInstalledGrant: ' Then grant Accessibility and Screen Recording here.',
+      notReady: 'Not ready',
+      notSupported: platform => `Computer Use isn't supported on this platform (${platform}).`,
+      platformNoteLinux: 'Drives your desktop via the X11/XWayland accessibility stack тАФ no permission prompt.',
+      platformNoteWin:
+        'First run may trigger a Windows SmartScreen prompt for the cua-driver UIAccess worker тАФ allow it.',
+      ready: 'Ready',
+      readyMessage: 'Computer Use is ready. Ask the agent to capture an app and click around.',
+      recheck: 'Recheck',
+      screenRecording: 'Screen Recording',
+      screenRecordingHint: 'Lets cua-driver capture screenshots of app windows.',
+      unknown: 'Unknown',
+      waitingApproval: 'Waiting for approvalтАж',
+    },
+    billingPage: {
+      alreadyOnPlan: (name: string) => `You are already on ${name} тАФ nothing to change.`,
+      autoRefillReloadTo: 'Reload to',
+      autoRefillReloadToAria: 'Auto-refill reload-to amount',
+      autoRefillThreshold: 'Threshold',
+      autoRefillThresholdAria: 'Auto-refill threshold',
+      autoRefillTurnedOff: 'Auto-refill turned off.',
+      autoRefillUpdated: 'Auto-refill updated.',
+      backToBilling: 'Back to billing',
+      cannotChange: 'That change cannot be made here.',
+      cannotSchedule: 'This change cannot be scheduled here.',
+      changeTakesEffect: (name: string, date: string, creditsDelta: string) =>
+        `Change to ${name} тАФ takes effect ${date}. No charge now; you keep your current plan until then.${
+          creditsDelta ? ` Monthly credits change: ${creditsDelta}.` : ''
+        }`,
+      checkingChange: 'Checking this changeтАж',
+      confirmDowngrade: 'Confirm downgrade',
+      currentPlan: 'Current plan',
+      customCreditAmountAria: 'Custom credit amount',
+      devFixture: 'Billing preview fixture (dev only)',
+      disable: 'Disable',
+      dismiss: 'Dismiss',
+      downgrade: 'Downgrade',
+      invoices: 'Invoices',
+      openPortal: 'Open portal',
+      openVerificationPage: 'Open verification page',
+      paymentCredits: 'Payment & credits',
+      plan: 'Plan',
+      plans: 'Plans',
+      scheduled: 'Scheduled',
+      scheduling: 'SchedulingтАж',
+      tryAgain: 'Try again',
+      turnOff: 'Turn off',
+      turnOffAutoRefill: 'Turn off auto-refill?',
+      undo: 'Undo',
+      undoing: 'UndoingтАж',
+      usage: 'Usage',
+      verifyToContinue: 'Verify to continue',
+      waitingForVerification: 'Waiting for verification linkтАж',
+      manage: 'Manage',
+      noPlansAvailable: 'No plans are available to change to right now.',
+    },
+    uninstall: {
+      appPath: path => `App: ${path}`,
+      checking: "Checking what's installedтАж",
+      confirmDesc: consequence => `This removes ${consequence}. This can't be undone.`,
+      confirmTitle: 'Confirm uninstall',
+      couldNotStart: 'Uninstall could not start.',
+      heading: 'Danger zone',
+      intro:
+        'Choose how much to remove. The app closes to finish the job; reopen the installer any time to come back.',
+      optionFull: 'Uninstall everything',
+      optionFullConsequence:
+        'EVERYTHING тАФ the Chat GUI, the Hermes agent, and all of your config, chats, secrets, and logs',
+      optionFullDesc:
+        'Remove the app, the agent, and all user data тАФ config, chats, scheduled jobs, secrets, logs.',
+      optionGui: 'Uninstall Chat GUI only',
+      optionGuiConsequence: 'the desktop Chat GUI (this app and its data)',
+      optionGuiDesc: 'Remove this desktop app. The Hermes agent, your config, and chats all stay.',
+      optionLite: 'Uninstall GUI + agent, keep my data',
+      optionLiteConsequence: 'the Chat GUI and the Hermes agent (config, chats, and secrets are kept)',
+      optionLiteDesc:
+        'Remove the app and the Hermes agent, but keep config, chats, and secrets for a future reinstall.',
+      title: 'Uninstall Hermes',
+      uninstalling: 'UninstallingтАж',
+      yes: 'Yes, uninstall',
+      checkingInstalled: 'Checking whatтАЩs installedтАж',
+    },
   },
 
   skills: {
@@ -1229,7 +1339,10 @@ export const en: Translations = {
       referenceImageTooLarge: 'Reference image is too large. Use one under 16 MB.',
       referenceImageInvalid: 'Could not read that reference image. Try a PNG, JPG, WebP, or GIF.',
       adopt: 'Adopt',
-      startOver: 'Start over'
+      startOver: 'Start over',
+      setupImageGeneration: 'Set up image generation',
+      removeReference: 'Remove reference',
+      grabKeyFrom: 'Grab a key from',
     },
     installTheme: {
       title: 'Install themeтАж',
@@ -2775,7 +2888,13 @@ export const en: Translations = {
       restoreNext: 'Restore next checkpoint',
       goForward: 'Go forward',
       sendEdited: 'Send edited message',
-      attachingFile: 'AttachingтАж'
+      attachingFile: 'AttachingтАж',
+      searchReactions: 'SearchтАж',
+      reactedByHermes: 'Reacted by Hermes',
+      noEmoji: 'No emoji found.',
+      moreEmoji: 'More emoji',
+      loadingEmoji: 'Loading emojiтАж',
+      conversationTimeline: 'Conversation timeline',
     },
     approval: {
       gatewayDisconnected: 'Hermes gateway is not connected',
@@ -3002,5 +3121,13 @@ export const en: Translations = {
       description: 'Displays the mobile sidebar.',
       toggle: open => `${open ? 'Show' : 'Hide'} sidebar`
     }
-  }
+  },
+  embeds: {
+    spotify: 'Spotify embed',
+    youtube: 'YouTube embed'
+  },
+  pet: {
+    hatchingProgress: 'Hatching progress',
+    openInHermes: 'Open in Hermes'
+  },
 }
diff --git a/apps/desktop/src/i18n/languages.ts b/apps/desktop/src/i18n/languages.ts
index e4a1fb1e02..a1f085bd92 100644
--- a/apps/desktop/src/i18n/languages.ts
+++ b/apps/desktop/src/i18n/languages.ts
@@ -34,6 +34,13 @@ export const LOCALE_OPTIONS = [
     name: '╪з┘Д╪╣╪▒╪и┘К╪й',
     englishName: 'Arabic',
     configValue: 'ar'
+  },
+  // MOD: hermes-desktop-ru тАФ Russian locale
+  {
+    id: 'ru',
+    name: '╨а╤Г╤Б╤Б╨║╨╕╨╣',
+    englishName: 'Russian',
+    configValue: 'ru'
   }
 ] as const satisfies readonly { configValue: string; englishName: string; id: Locale; name: string }[]
 
@@ -79,7 +86,13 @@ const LOCALE_ALIASES: Record<string, Locale> = {
   'ar-eg': 'ar',
   ar_eg: 'ar',
   arabic: 'ar',
-  ╪з┘Д╪╣╪▒╪и┘К╪й: 'ar'
+  ╪з┘Д╪╣╪▒╪и┘К╪й: 'ar',
+  // MOD: hermes-desktop-ru
+  ru: 'ru',
+  'ru-ru': 'ru',
+  ru_ru: 'ru',
+  russian: 'ru',
+  ╤А╤Г╤Б╤Б╨║╨╕╨╣: 'ru'
 }
 
 export function isLocale(value: unknown): value is Locale {
diff --git a/apps/desktop/src/i18n/types.ts b/apps/desktop/src/i18n/types.ts
index d611086143..0e9e4462b6 100644
--- a/apps/desktop/src/i18n/types.ts
+++ b/apps/desktop/src/i18n/types.ts
@@ -5,7 +5,7 @@
 // partial locales should use `defineLocale()` so missing desktop-only strings
 // fall back to English while new keys remain type-checked.
 
-export type Locale = 'en' | 'zh' | 'zh-hant' | 'ja' | 'ar'
+export type Locale = 'en' | 'zh' | 'zh-hant' | 'ja' | 'ar' | 'ru'
 
 export type ToolTitleKey =
   | 'browser_click'
@@ -89,6 +89,10 @@ export interface Translations {
     tryHint: (term: string) => string
     on: string
     off: string
+    use: string,
+    showOptions: string,
+    moreActions: string,
+    dismiss: string,
   }
 
   fileMenu: {
@@ -316,6 +320,7 @@ export interface Translations {
       disable: string
       failed: string
       empty: string
+      kanban: string
       kinds: { bundled: string; disk: string; runtime: string }
       agent: {
         title: string
@@ -329,6 +334,7 @@ export interface Translations {
         updateBackendToManage: string
         sources: Record<string, string>
       }
+      descriptions: Record<string, string>,
     }
     notifications: {
       title: string
@@ -431,6 +437,7 @@ export interface Translations {
         turnOnFailed: string
         turnOffFailed: string
       }
+      searchThemes: string,
     }
     fieldLabels: Record<string, string>
     fieldDescriptions: Record<string, string>
@@ -492,6 +499,9 @@ export interface Translations {
       active: string
       takenBy: string
       invalidShortcut: string
+      windowAriaLabel: string,
+      newTarget: string,
+      currentTarget: string,
     }
     credentials: {
       pasteKey: string
@@ -865,6 +875,94 @@ export interface Translations {
         needsSetupHint: string
       }
     }
+    billingPage: {
+      alreadyOnPlan: (name: string) => string,
+      autoRefillReloadTo: string,
+      autoRefillReloadToAria: string,
+      autoRefillThreshold: string,
+      autoRefillThresholdAria: string,
+      autoRefillTurnedOff: string,
+      autoRefillUpdated: string,
+      backToBilling: string,
+      cannotChange: string,
+      cannotSchedule: string,
+      changeTakesEffect: (name: string, date: string, creditsDelta: string) => string,
+      checkingChange: string,
+      confirmDowngrade: string,
+      currentPlan: string,
+      customCreditAmountAria: string,
+      devFixture: string,
+      disable: string,
+      dismiss: string,
+      downgrade: string,
+      invoices: string,
+      manage: string,
+      noPlansAvailable: string,
+      openPortal: string,
+      openVerificationPage: string,
+      paymentCredits: string,
+      plan: string,
+      plans: string,
+      scheduled: string,
+      scheduling: string,
+      tryAgain: string,
+      turnOff: string,
+      turnOffAutoRefill: string,
+      undo: string,
+      undoing: string,
+      usage: string,
+      verifyToContinue: string,
+      waitingForVerification: string,
+    },
+    computerUse: {
+      accessibility: string,
+      accessibilityHint: string,
+      approveMessage: string,
+      approveTitle: string,
+      checking: string,
+      driverHealth: string,
+      errorRead: string,
+      errorRequest: string,
+      grantPermissions: string,
+      granted: string,
+      grantsNote: string,
+      notGranted: string,
+      notInstalled: string,
+      notInstalledGrant: string,
+      notReady: string,
+      notSupported: (...args: unknown[]) => string,
+      platformNoteLinux: string,
+      platformNoteWin: string,
+      ready: string,
+      readyMessage: string,
+      recheck: string,
+      screenRecording: string,
+      screenRecordingHint: string,
+      unknown: string,
+      waitingApproval: string,
+    },
+    uninstall: {
+      appPath: (...args: unknown[]) => string,
+      checking: string,
+      checkingInstalled: string,
+      confirmDesc: (...args: unknown[]) => string,
+      confirmTitle: string,
+      couldNotStart: string,
+      heading: string,
+      intro: string,
+      optionFull: string,
+      optionFullConsequence: string,
+      optionFullDesc: string,
+      optionGui: string,
+      optionGuiConsequence: string,
+      optionGuiDesc: string,
+      optionLite: string,
+      optionLiteConsequence: string,
+      optionLiteDesc: string,
+      title: string,
+      uninstalling: string,
+      yes: string,
+    },
   }
 
   skills: {
@@ -1083,6 +1181,9 @@ export interface Translations {
       referenceImageInvalid: string
       adopt: string
       startOver: string
+      setupImageGeneration: string,
+      removeReference: string,
+      grabKeyFrom: string,
     }
     installTheme: {
       title: string
@@ -2364,6 +2465,12 @@ export interface Translations {
       goForward: string
       sendEdited: string
       attachingFile: string
+      searchReactions: string,
+      reactedByHermes: string,
+      noEmoji: string,
+      moreEmoji: string,
+      loadingEmoji: string,
+      conversationTimeline: string,
     }
     approval: {
       gatewayDisconnected: string
@@ -2553,4 +2660,12 @@ export interface Translations {
       toggle: (open: boolean) => string
     }
   }
+  embeds: {
+    spotify: string,
+    youtube: string,
+  },
+  pet: {
+    hatchingProgress: string,
+    openInHermes: string,
+  },
 }
diff --git a/apps/desktop/src/lib/chat-runtime.ts b/apps/desktop/src/lib/chat-runtime.ts
index 4f48a141a9..9e3406b801 100644
--- a/apps/desktop/src/lib/chat-runtime.ts
+++ b/apps/desktop/src/lib/chat-runtime.ts
@@ -48,12 +48,12 @@ export function createClientSessionState(
 }
 
 export function sessionTitle(session: SessionInfo): string {
-  return session.title?.trim() || session.preview?.trim() || 'Untitled session'
+  return session.title?.trim() || session.preview?.trim() || '╨С╨╡╨╖ ╨╜╨░╨╖╨▓╨░╨╜╨╕╤П'
 }
 
 /** What a session is called before it has been sent тАФ and before its composer
  *  has been typed into, which is the only thing that can name it earlier. */
-export const NEW_SESSION_TITLE = 'New session'
+export const NEW_SESSION_TITLE = '╨Э╨╛╨▓╨░╤П ╤Б╨╡╤Б╤Б╨╕╤П'
 
 export function coerceGatewayText(value: unknown): string {
   if (typeof value === 'string') {
diff --git a/apps/desktop/src/plugins/kanban/plugin.tsx b/apps/desktop/src/plugins/kanban/plugin.tsx
index 605f58ad3c..f76dc35761 100644
--- a/apps/desktop/src/plugins/kanban/plugin.tsx
+++ b/apps/desktop/src/plugins/kanban/plugin.tsx
@@ -33,6 +33,8 @@ import {
 import { $boardSlug, bindApi, boardKey, fetchBoard } from './api'
 import { KanbanBoardPage } from './board'
 import { KANBAN_LOCALES } from './i18n'
+// MOD: hermes-desktop-ru тАФ Russian kanban locale (registered on import)
+import './ru-locales'
 import { $newTaskLane, useKanban } from './ui'
 
 // Live "N running / ready" pill тАФ one glance at fleet activity from anywhere,
@@ -80,7 +82,7 @@ function KanbanCount() {
 const plugin: HermesPlugin = {
   id: 'kanban',
   name: 'Kanban',
-  description: 'Multi-agent task board тАФ board page, sidebar entry, and a live in-flight count in the status bar.',
+  description: '╨Ь╨╜╨╛╨│╨╛╨╖╨░╨┤╨░╤З╨╜╨░╤П ╨┤╨╛╤Б╨║╨░ ╨░╨│╨╡╨╜╤В╨╛╨▓ тАФ ╤Б╤В╤А╨░╨╜╨╕╤Ж╨░ ╨┤╨╛╤Б╨║╨╕, ╨╖╨░╨┐╨╕╤Б╤М ╨▓ ╤Б╨░╨╣╨┤╨▒╨░╤А╨╡ ╨╕ ╨╢╨╕╨▓╨╛╨╣ ╤Б╤З╤С╤В╤З╨╕╨║ ╨▓ ╤Б╤В╨░╤В╤Г╤Б-╨▒╨░╤А╨╡.',
   defaultEnabled: false,
   register(ctx) {
     ctx.i18n.register(KANBAN_LOCALES)
